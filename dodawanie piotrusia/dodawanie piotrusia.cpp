#include <iostream>
using namespace std;

void sumuj(int l)
{
	int *tab;
	tab=new int[]
}
int main ()
{
	int nr,ile,liczba,l2,suma,j,d;
	cin>>nr;
	for(int i=0;i<nr;i++)
	{
		for(int j=0;j<ile;j++)
		{
			cin>>liczba;
			l2=liczba;
			j=liczba%10;
			liczba/=10;
			d=liczba%10;
			if (j==d)
			{
				cout<<j+d<<" "<<1<<endl;
			}
			else
			{
				suma=sumuj(l2);
				while(!czypalindrom(suma))
			}
		}
	}
}
